# ============================================================
# Tickr 환경변수 예시
# 실제 값은 .env (gitignored) 에 작성. 이 파일은 키 목록 참조용.
# ============================================================

# ─── Supabase (apps/api 서버 전용) ─────────────────────────
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# ─── KIS Developers OpenAPI (apps/api 서버 전용) ──────────
# KIS_USE_MOCK=true 이면 모의투자 서버 사용, false 이면 실전 서버 사용
KIS_USE_MOCK=true
KIS_APP_KEY=
KIS_APP_SECRET=
# 실전: https://openapi.koreainvestment.com:9443
# 모의: https://openapivts.koreainvestment.com:29443
KIS_BASE_URL=https://openapivts.koreainvestment.com:29443
# 개발자 본인 모의계좌 (시세 조회/주문 호출 테스트용, 종합계좌번호 8자리)
KIS_ACCOUNT_NO=
# 계좌상품코드 (모의·실전 모두 종합주식 = 01)
KIS_ACCOUNT_PROD_CD=01

# ─── API 서버 자체 설정 ────────────────────────────────────
API_PORT=4000
NODE_ENV=development

# ─── apps/mobile (Expo) ───────────────────────────────────
# EXPO_PUBLIC_ 접두사가 붙은 변수만 클라이언트 번들에 노출됨.
# 절대 SERVICE_ROLE 같은 비밀키는 EXPO_PUBLIC_* 에 두지 말 것.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_API_URL=http://localhost:4000
EXPO_PUBLIC_WS_URL=ws://localhost:4000/ws
